import { Box, styled } from '@mui/material';

export const FormStyled = styled(Box)(() => ({
    flex: '0 0 auto',
    padding: 20,
    border: '1px grey solid',
    borderRadius: 8,
    margin: 8,

    background: 'rgba(50, 80, 220, 0.2)',
}));
